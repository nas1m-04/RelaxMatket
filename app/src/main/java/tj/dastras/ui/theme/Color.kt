package tj.dastras.ui.theme

import androidx.compose.ui.graphics.Color

// ── RELAX Brand Core ──────────────────────────────────────────
val RelaxDark          = Color(0xFF0F172A)
val RelaxDarkSecondary = Color(0xFF1E293B)
val RelaxRed           = Color(0xFFE53935)
val RelaxOrange        = Color(0xFFFF6B35)

// ── Surface & Background ──────────────────────────────────────
val RelaxWhite         = Color(0xFFFFFFFF)
val RelaxBackground    = Color(0xFFF8F9FA)
val RelaxSurface       = Color(0xFFFFFFFF)
val RelaxSurfaceAlt    = Color(0xFFF3F4F6)
val RelaxInputBg       = Color(0xFFF1F5F9)

// ── Text ──────────────────────────────────────────────────────
val RelaxTextPrimary   = Color(0xFF111827)
val RelaxTextSecondary = Color(0xFF6B7280)
val RelaxTextHint      = Color(0xFF9CA3AF)
val RelaxTextOnDark    = Color(0xFFFFFFFF)
val RelaxTextOnDarkSub = Color(0xFFCBD5E1)

// ── Semantic ──────────────────────────────────────────────────
val RelaxSuccess       = Color(0xFF22C55E)
val RelaxSuccessBg     = Color(0xFFDCFCE7)
val RelaxWarning       = Color(0xFFF59E0B)
val RelaxError         = Color(0xFFEF4444)

// ── UI Elements ───────────────────────────────────────────────
val RelaxDivider       = Color(0xFFE5E7EB)
val RelaxBorder        = Color(0xFFD1D5DB)
val RelaxShadow        = Color(0x14000000)
val RelaxOverlay       = Color(0x80000000)

// ── Loyalty ───────────────────────────────────────────────────
val RelaxGold          = Color(0xFFD4AF37)
val RelaxGoldLight     = Color(0xFFFFF8DC)
val RelaxSilver        = Color(0xFFC0C0C0)

// ── Category chips ────────────────────────────────────────────
val CatMeat            = Color(0xFFFFE5E5)
val CatVegetables      = Color(0xFFE5F5E5)
val CatFruits          = Color(0xFFFFF3E0)
val CatDairy           = Color(0xFFE5F0FF)
val CatDrinks          = Color(0xFFEDE5FF)
val CatChemistry       = Color(0xFFFFE5F5)
val CatBaby            = Color(0xFFFFF5E5)
val CatBakery          = Color(0xFFFFF9E5)
