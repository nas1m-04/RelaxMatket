<resources>
    <!-- Only the app label AndroidManifest.xml needs — native Android resources (AAPT), separate
    from the Compose Multiplatform string table in commonMain/composeResources. Not referenced from
    any Compose code; kept out of the CMP copy to avoid two sources of truth for the same string. -->
    <string name="app_name">Relax</string>
</resources>
